'use strict';

let Chain = require('./model/chain');
let Block = require('./model/block');


// let testChain = new Chain();
// // testChain.currentChainArray = ['happy'];
// testChain.currentChainArray.push(new Block (0, 'genesis', new Date(), 'ledger', 'asdfgasdfasdgasdf'));
// // testChain.makeBlockHash(1, 2, 3, 4);
// testChain.currentChainArray.push(testChain.makeNextBlock('sethIsBadAtPingPong, worse than nick'));
// console.log(testChain.currentChainArray);
//
// // console.log('block is :', block);
